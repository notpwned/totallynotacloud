import SwiftUI

struct AppColors {
    static let background = Color(red: 0.04, green: 0.04, blue: 0.04)
    static let surface = Color(red: 0.08, green: 0.08, blue: 0.08)
    static let surfaceLight = Color(red: 0.12, green: 0.12, blue: 0.12)
    
    static let textPrimary = Color.white
    static let textSecondary = Color(white: 0.5)
    static let textTertiary = Color(white: 0.35)
    
    static let accent = Color.white
    static let accentDim = Color(white: 0.8)
    
    static let divider = Color(white: 0.15)
    static let success = Color(white: 0.9)
    static let error = Color(white: 0.85)
}
