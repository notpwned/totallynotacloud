# Development Guide

## Требования

- **macOS**: 13.0 или выше
- **Xcode**: 15.0 или выше
- **iOS**: 16.0 или выше
- **Swift**: 5.9 или выше

## Структура проекта

```
iOS/
├── App/
│   ├── totallynotacloudApp.swift      # Главная точка входа
│   └── AppColors.swift                # Цветовая схема
│
├── Models/
│   ├── CloudFile.swift                # Модель файла
│   └── User.swift                     # Модель пользователя
│
├── Services/
│   ├── AuthService.swift              # Сервис аутентификации
│   └── StorageService.swift           # Сервис управления файлами
│
└── Views/
    ├── AuthView.swift                 # Экран входа/регистрации
    ├── MainTabView.swift              # Главное меню с вкладками
    ├── FilesView.swift                # Список файлов
    ├── StorageView.swift              # Статистика хранилища
    └── ProfileView.swift              # Профиль пользователя
```

## Установка

### 1. Клонирование репозитория

```bash
git clone https://github.com/notpwned/totallynotacloud.git
cd totallynotacloud
```

### 2. Открытие в Xcode

```bash
xed iOS/
```

Или просто дважды кликните на `totallynotacloud.xcodeproj`

### 3. Выбор целевой платформы

- **iOS**: Выберите iPhone 15 Pro симулятор
- **macOS**: Выберите My Mac (Designed for iPad)

### 4. Запуск проекта

```
Cmd + R
```

## Архитектура

### MVVM паттерн

- **Model**: `CloudFile`, `User` - структуры данных
- **ViewModel**: `AuthService`, `StorageService` - логика и состояние (@Published)
- **View**: SwiftUI компоненты для отображения

### Сервисы

#### AuthService

Отвечает за:
- Аутентификацию пользователя
- Управление токеном доступа (в UserDefaults)
- Загрузку профиля пользователя

```swift
await authService.login(email: "user@example.com", password: "password")
await authService.register(email: "new@example.com", username: "username", password: "password")
authService.logout()
```

#### StorageService

Отвечает за:
- Загрузку списка файлов
- Загрузку файлов
- Удаление файлов
- Создание папок
- Отслеживание прогресса загрузки

```swift
await storageService.loadFiles()
await storageService.uploadFile(data: data, name: "file.pdf", mimeType: "application/pdf")
await storageService.deleteFile(file)
await storageService.createFolder(name: "New Folder")
```

## Цветовая схема

Дизайн использует тёмный минималистичный стиль с белыми и синими акцентами:

- **Background**: `#0D0D10` (очень тёмный серый)
- **Surface**: `#1B1B21` (тёмный серый)
- **Accent Blue**: `#0077FF` (iOS синий)
- **Text Primary**: `#FFFFFF` (белый)
- **Text Secondary**: `#A6A6A6` (серый)

## Следующие шаги

### Краткосрочные задачи

1. **Реальная авторизация**
   - Интегрировать REST API
   - Добавить JWT токены
   - Реализовать безопасное хранилище токенов (Keychain)

2. **Загрузка файлов**
   - Интегрировать DocumentPickerViewController
   - Реализовать загрузку на сервер
   - Добавить сжатие изображений

3. **macOS версия**
   - Адаптировать UI для больших экранов
   - Добавить drag & drop поддержку
   - Реализовать сохранение файлов

### Среднесрочные задачи

1. **Backend разработка**
   - REST API с Node.js/Express или Python/FastAPI
   - PostgreSQL для хранения данных
   - S3/MinIO для хранения файлов

2. **Синхронизация**
   - CloudKit для синхронизации между устройствами
   - Offline-first подход
   - Conflict resolution

3. **Безопасность**
   - E2E шифрование файлов
   - 2FA поддержка
   - Логирование и аудит

### Долгосрочные задачи

1. **Веб-интерфейс**
   - React приложение для браузера
   - Синхронизация с мобильным клиентом

2. **Веб-сайт**
   - Лендинг страница
   - Документация
   - Блог

3. **Масштабирование**
   - Кэширование
   - CDN для раздачи файлов
   - Analytics

## Отладка

### Включение mock данных

Мок-данные автоматически загружаются в `StorageService` при инициализации.

### Логирование

Добавьте логирование в сервисы:

```swift
print("🔐 Auth: Login attempt for \(email)")
print("📁 Storage: Loaded \(files.count) files")
```

### Тестирование

Для тестирования используйте mock аккаунт:
- Email: `test@example.com`
- Password: `password123`

## Полезные ресурсы

- [SwiftUI Documentation](https://developer.apple.com/xcode/swiftui/)
- [Combine Framework](https://developer.apple.com/documentation/combine)
- [Swift Concurrency](https://developer.apple.com/documentation/swift/concurrency)
- [iOS Security Best Practices](https://developer.apple.com/security/)

## Контрибьютирование

1. Fork репозиторий
2. Создайте feature branch (`git checkout -b feature/amazing-feature`)
3. Commit изменения (`git commit -m 'Add amazing feature'`)
4. Push в branch (`git push origin feature/amazing-feature`)
5. Откройте Pull Request

## Лицензия

MIT License - смотрите файл LICENSE

---

**Статус**: 🚀 В активной разработке
